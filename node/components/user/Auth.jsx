var React = require('react');
var Dialog = require('../lib/dialog/Dialog');
require('./Auth.scss');

module.exports = React.createClass({
    render: function() {
        return (
            <Dialog/>
            );
    }
});